import { TopBar } from '@/components/layout/topbar';

export default function PendentesPage() {
  return (
    <>
      <TopBar title="Pendentes" subtitle="Em breve — Fase futura do plano" />
      <div style={{ padding: '32px 40px', color: 'var(--text-secondary)' }}>
        Esta seção ainda não foi construída. Volte quando as próximas fases do CRM chegarem.
      </div>
    </>
  );
}
